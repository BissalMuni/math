import { ComingSoon } from "@/components/content/shared";

/** Ⅰ. GPU·AI 가속기 — hermes 봇이 채울 자리 (스켈레톤 스텁) */
export default function AdcGpuAccelerator() {
  return (
    <div className="space-y-8">
      <ComingSoon title="Ⅰ. GPU·AI 가속기" />
    </div>
  );
}
