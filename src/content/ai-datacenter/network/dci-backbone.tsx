import { ComingSoon } from "@/components/content/shared";

/** Ⅰ. 데이터센터 간 연결(DCI)·백본 — hermes 봇이 채울 자리 (스켈레톤 스텁) */
export default function AdcDciBackbone() {
  return (
    <div className="space-y-8">
      <ComingSoon title="Ⅰ. 데이터센터 간 연결(DCI)·백본" />
    </div>
  );
}
