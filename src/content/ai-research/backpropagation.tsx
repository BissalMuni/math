import { ComingSoon } from "@/components/content/shared";

/** Ⅳ. 역전파 — 다층 신경망 학습 — hermes 봇이 채울 자리 (스켈레톤 스텁) */
export default function AirBackpropagation() {
  return (
    <div className="space-y-8">
      <ComingSoon title="Ⅳ. 역전파 — 다층 신경망 학습" />
    </div>
  );
}
