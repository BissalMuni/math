import { ComingSoon } from "@/components/content/shared";

/** ⅩⅣ. ELMo — 문맥적 임베딩 — hermes 봇이 채울 자리 (스켈레톤 스텁) */
export default function AirElmo() {
  return (
    <div className="space-y-8">
      <ComingSoon title="ⅩⅣ. ELMo — 문맥적 임베딩" />
    </div>
  );
}
