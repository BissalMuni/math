import { ComingSoon } from "@/components/content/shared";

/** ⅩⅥ. GPT-1 — 생성적 사전학습 — hermes 봇이 채울 자리 (스켈레톤 스텁) */
export default function AirGpt1() {
  return (
    <div className="space-y-8">
      <ComingSoon title="ⅩⅥ. GPT-1 — 생성적 사전학습" />
    </div>
  );
}
