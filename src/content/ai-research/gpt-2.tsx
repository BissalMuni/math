import { ComingSoon } from "@/components/content/shared";

/** ⅩⅦ. GPT-2 — 비지도 멀티태스크 — hermes 봇이 채울 자리 (스켈레톤 스텁) */
export default function AirGpt2() {
  return (
    <div className="space-y-8">
      <ComingSoon title="ⅩⅦ. GPT-2 — 비지도 멀티태스크" />
    </div>
  );
}
