import { ComingSoon } from "@/components/content/shared";

/** ⅩⅧ. GPT-3 — few-shot 학습 — hermes 봇이 채울 자리 (스켈레톤 스텁) */
export default function AirGpt3() {
  return (
    <div className="space-y-8">
      <ComingSoon title="ⅩⅧ. GPT-3 — few-shot 학습" />
    </div>
  );
}
