import { ComingSoon } from "@/components/content/shared";

/** ⅩⅩⅠ. GPT-4 — hermes 봇이 채울 자리 (스켈레톤 스텁) */
export default function AirGpt4() {
  return (
    <div className="space-y-8">
      <ComingSoon title="ⅩⅩⅠ. GPT-4" />
    </div>
  );
}
