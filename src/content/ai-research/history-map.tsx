import { ComingSoon } from "@/components/content/shared";

/** Ⅱ. AI 발전 지도 — 전체 개관 — hermes 봇이 채울 자리 (스켈레톤 스텁) */
export default function AirHistoryMap() {
  return (
    <div className="space-y-8">
      <ComingSoon title="Ⅱ. AI 발전 지도 — 전체 개관" />
    </div>
  );
}
