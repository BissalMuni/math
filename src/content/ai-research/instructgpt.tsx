import { ComingSoon } from "@/components/content/shared";

/** ⅩⅩ. InstructGPT — RLHF 정렬 — hermes 봇이 채울 자리 (스켈레톤 스텁) */
export default function AirInstructgpt() {
  return (
    <div className="space-y-8">
      <ComingSoon title="ⅩⅩ. InstructGPT — RLHF 정렬" />
    </div>
  );
}
