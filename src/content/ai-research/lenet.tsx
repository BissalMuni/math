import { ComingSoon } from "@/components/content/shared";

/** Ⅵ. LeNet — CNN의 탄생 — hermes 봇이 채울 자리 (스켈레톤 스텁) */
export default function AirLenet() {
  return (
    <div className="space-y-8">
      <ComingSoon title="Ⅵ. LeNet — CNN의 탄생" />
    </div>
  );
}
