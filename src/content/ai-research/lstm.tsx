import { ComingSoon } from "@/components/content/shared";

/** Ⅸ. LSTM — 장기 의존성 — hermes 봇이 채울 자리 (스켈레톤 스텁) */
export default function AirLstm() {
  return (
    <div className="space-y-8">
      <ComingSoon title="Ⅸ. LSTM — 장기 의존성" />
    </div>
  );
}
