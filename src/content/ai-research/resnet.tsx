import { ComingSoon } from "@/components/content/shared";

/** Ⅷ. ResNet — 깊이의 돌파 — hermes 봇이 채울 자리 (스켈레톤 스텁) */
export default function AirResnet() {
  return (
    <div className="space-y-8">
      <ComingSoon title="Ⅷ. ResNet — 깊이의 돌파" />
    </div>
  );
}
