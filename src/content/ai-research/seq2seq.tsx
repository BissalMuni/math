import { ComingSoon } from "@/components/content/shared";

/** Ⅺ. Seq2Seq — 인코더·디코더 — hermes 봇이 채울 자리 (스켈레톤 스텁) */
export default function AirSeq2seq() {
  return (
    <div className="space-y-8">
      <ComingSoon title="Ⅺ. Seq2Seq — 인코더·디코더" />
    </div>
  );
}
