import { ComingSoon } from "@/components/content/shared";

/** Ⅴ. SVM — 고전 머신러닝의 정점 — hermes 봇이 채울 자리 (스켈레톤 스텁) */
export default function AirSvm() {
  return (
    <div className="space-y-8">
      <ComingSoon title="Ⅴ. SVM — 고전 머신러닝의 정점" />
    </div>
  );
}
