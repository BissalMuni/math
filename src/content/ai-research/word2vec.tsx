import { ComingSoon } from "@/components/content/shared";

/** Ⅹ. Word2Vec — 단어 임베딩 — hermes 봇이 채울 자리 (스켈레톤 스텁) */
export default function AirWord2vec() {
  return (
    <div className="space-y-8">
      <ComingSoon title="Ⅹ. Word2Vec — 단어 임베딩" />
    </div>
  );
}
