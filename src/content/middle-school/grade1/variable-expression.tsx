"use client";

import { CalcBox } from "@/components/content/shared";

/** 중학교 수학 > 중학교 1학년 > Ⅱ. 문자와 식 > 1. 문자의 사용과 식의 계산 > 1. 문자의 사용과 식의 계산 */
export default function VariableExpression() {
  return (
    <div className="space-y-8">
      <CalcBox title="1. 문자의 사용">
        <p className="text-muted italic">콘텐츠 준비 중입니다.</p>
      </CalcBox>

      <CalcBox title="2. 식의 값">
        <p className="text-muted italic">콘텐츠 준비 중입니다.</p>
      </CalcBox>

      <CalcBox title="3. 일차식의 계산">
        <p className="text-muted italic">콘텐츠 준비 중입니다.</p>
      </CalcBox>
    </div>
  );
}
