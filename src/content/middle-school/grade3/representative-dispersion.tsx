"use client";

import { CalcBox } from "@/components/content/shared";

/** 중학교 수학 > 중학교 3학년 > Ⅵ. 통계 > 1. 대푯값과 산포도 > 1. 대푯값과 산포도 */
export default function RepresentativeDispersion() {
  return (
    <div className="space-y-8">
      <CalcBox title="1. 평균, 중앙값, 최빈값">
        <p className="text-muted italic">콘텐츠 준비 중입니다.</p>
      </CalcBox>

      <CalcBox title="2. 분산과 표준편차">
        <p className="text-muted italic">콘텐츠 준비 중입니다.</p>
      </CalcBox>
    </div>
  );
}
